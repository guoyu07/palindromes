<!DOCTYPE html>
<html>
<head>
    <link rel='stylesheet' href='https://maxcdn.bootstrapcdn.com/bootstrap/3.1.1/css/bootstrap.min.css'>
    <title>Palindromes</title>
</head>
<body>
    <div class='container'>
        <h1>Palindromes</h1>
        {% if letters is empty %}
        <p>negativo. that was not a palindrome.</p>
        {% else %}
        {% for letter in letters %}
            <p>{{ letters }}</p>
        {% endfor %}
        {% endif %}
        <a href='/'>return home</a>
    </div>
</body>
</html>
